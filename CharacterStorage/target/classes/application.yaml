spring:
  datasource: 
    username: character_storage
    password: character_storage
    url: jdbc:mysql://localhost:3307/character_storage
    
       
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    defer-datasource-initialization: true
  sql:
    init:
      mode: never